# =============================================================
# THR Review — Build and Deploy
# =============================================================
#
# WHAT THIS DOES:
# 1. Installs deps and builds the SPA with bun (on the runner)
# 2. Builds a slim nginx Docker image with the pre-built assets
# 3. Pushes to GitHub Container Registry (ghcr.io)
# 4. SSHs into the VPS and pulls the new image
# 5. Restarts the review container and runs health check
#
# WHY BUILD OUTSIDE DOCKER:
# TanStack Start SPA mode prerenders a shell HTML by spinning up
# a temporary SSR server. This doesn't work inside Docker build
# (no localhost networking). Building on the runner avoids this.
#
# WHEN IT RUNS:
# - Automatically when you push to thr-main
# - Manually via the "Run workflow" button
#
# SECRETS NEEDED (set in GitHub repo Settings > Secrets):
# - VPS_SSH_KEY: Private SSH key for accessing the VPS
# - VPS_HOST: VPS IP address (148.230.85.40)
# - REVIEW_CLERK_PUBLISHABLE_KEY: Clerk publishable key (baked into SPA)
# =============================================================

name: Deploy THR Review

on:
  push:
    branches: [thr-main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: deploy-review
  cancel-in-progress: true

jobs:
  # ===========================================================
  # JOB 1: Build SPA, package into Docker, push to GHCR
  # ===========================================================
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build SPA
        env:
          VITE_CONVEX_URL: https://review-convex-api.truehandroofing.com
          VITE_CLERK_PUBLISHABLE_KEY: ${{ secrets.REVIEW_CLERK_PUBLISHABLE_KEY }}
        run: bun run build

      - name: Verify build output
        run: |
          ls -la dist/client/
          test -f dist/client/_shell.html && echo "_shell.html exists" || echo "WARNING: _shell.html missing"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push review image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ghcr.io/gitforgeai/thr-review:latest
            ghcr.io/gitforgeai/thr-review:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===========================================================
  # JOB 2: Deploy to VPS
  # ===========================================================
  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "=== Pulling THR Review image ==="
            cd /root/thr-deploy
            docker compose pull review
            docker compose up -d review
            echo "=== Review container restarted ==="
            docker ps --filter "name=thr-review" --format "table {{.Names}}\t{{.Status}}\t{{.Image}}"

      - name: Wait for service
        run: sleep 10

      - name: Health check
        run: |
          if curl -sf --max-time 15 -o /dev/null https://review.truehandroofing.com/healthz; then
            echo "THR Review is healthy"
          else
            echo "::warning::THR Review health check failed (may need DNS setup first)"
          fi
